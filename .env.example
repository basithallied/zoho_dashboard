# Copy to .env before running docker compose.

POSTGRES_DB=mis_agent
POSTGRES_USER=mis_admin
POSTGRES_PASSWORD=change-me

# Comma-separated origins allowed to call the API.
MIS_ALLOWED_ORIGINS=http://localhost

# Seconds between scheduler cycles (report generation, escalation, anomaly scan).
MIS_SCHEDULER_INTERVAL=900
